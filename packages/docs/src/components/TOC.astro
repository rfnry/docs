---
import TOC from "@rfnry/ui/components/TOC.astro";
import type { Heading } from "@rfnry/ui/lib/toc";

interface Props {
  headings: Heading[];
}
const { headings } = Astro.props;
---
<TOC headings={headings} contentSelector="article.prose h2[id], article.prose h3[id]" />
