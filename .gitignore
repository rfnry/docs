# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# Only pnpm-lock.yaml is committed as the canonical lockfile.
# npm / yarn users: generate and keep their lockfile locally.
package-lock.json
yarn.lock

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# reference template kept in-repo for comparison only
reference-docs/

/docs/plans